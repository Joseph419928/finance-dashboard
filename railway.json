{
  "$schema": "https://railway.app/railway.schema.json",
  "build": { "builder": "DOCKERFILE" },
  "deploy": {
    "startCommand": "node node_modules/prisma/build/index.js db push --skip-generate --accept-data-loss && { (node prisma/seed-prod.cjs || true) & echo '>>> starting next'; node node_modules/next/dist/bin/next start -H 0.0.0.0 -p ${PORT:-3000}; }",
    "restartPolicyType": "ON_FAILURE"
  }
}
