{
  "$schema": "https://railway.app/railway.schema.json",
  "build": { "builder": "NIXPACKS" },
  "deploy": {
    "startCommand": "{ (npx --no-install prisma db push --skip-generate --accept-data-loss && node prisma/seed-prod.cjs) || true; } & npx --no-install next start -H 0.0.0.0 -p ${PORT:-3000}",
    "restartPolicyType": "ON_FAILURE"
  }
}
