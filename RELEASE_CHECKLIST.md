# 上線檢查清單 — 匯入／帶入／選取式輸入（I1～I5）

> 適用版本：`IMPORT_CARRY_SPEC.md` 的 I1～I5 + 審核後的 FIX-0～FIX-8。
> **前提：CODEX 的 FIX-0～FIX-8 全部完成並回報通過後，才開始執行本清單。**
> 本次 schema 變更為純新增（`LineItem.source` 欄位 + `ItemPreset` 資料表），不刪不改既有欄位。

---

## 階段 0 — 事前確認（5 分鐘）

- [ ] CODEX 已回報 FIX-0～FIX-8 完成，且 `npx tsc --noEmit`、`npm run build` 皆通過
- [ ] `prisma/migrations/` 目錄**已刪除**（FIX-0：本專案採 `db push`，不使用 migration 歷史）
- [ ] `prisma/schema.prisma` 第 7 行仍為 `provider = "postgresql"`（測試期間若曾改成 sqlite 必須還原）
- [ ] 確認 Railway 服務的 `DATABASE_URL` 指向正式 PostgreSQL

---

## 階段 1 — 備份（**不可略過**）

### 1.1 取得正式資料庫連線字串

Railway → 你的 service → **Variables** → 複製 `DATABASE_URL` 的值。

### 1.2 匯出完整備份到本機

在本機 PowerShell 設定連線字串（把 `<貼上連線字串>` 換成上一步複製的值）：

```bash
$env:PGURL="<貼上連線字串>"
```

執行備份（檔名含日期，存到桌面）：

```bash
pg_dump $env:PGURL -Fc -f "$env:USERPROFILE\Desktop\fy_finance_backup_$(Get-Date -Format yyyyMMdd_HHmm).dump"
```

- [ ] 備份檔已產生，且檔案大小 > 0
- [ ] 備份檔已另存一份到 OneDrive 或其他位置

> 本機若沒有 `pg_dump`：安裝 PostgreSQL client 工具，或改用 Railway 的 **Database → Backups** 功能建立快照。
> 無論哪種方式，**沒有備份就不要往下走**。

### 1.3 記錄回滾指令（真的需要時才用）

```bash
pg_restore --clean --if-exists -d $env:PGURL "<備份檔完整路徑>"
```

---

## 階段 2 — 記錄基準數字（部署前）

用來比對「部署本身沒有改變任何財務數字」。請先打開網站，把下列數字抄下來：

| 項目 | 位置 | 部署前 | 部署後 |
|---|---|---|---|
| 2025 年度營業收入合計 | `/report/2025` | | |
| 2025 年度本期淨利 | `/report/2025` | | |
| 最近一個月的本期淨利 | `/monthly` 列表最上方 | | |
| 最近一個月的營業毛利 | `/monthly/[年]/[月]` | | |
| 儀表板 KPI 任一數字 | `/dashboard` | | |

- [ ] 五個數字已記錄

---

## 階段 3 — 部署

```bash
git add . && git commit -m "feat: 貨主/薪資匯入、固定支出續期、選取式名稱字典"
```

```bash
git push
```

Railway 會自動建置並重啟。啟動指令會執行 `prisma db push`，自動加上 `source` 欄位與 `ItemPreset` 表。

- [ ] Railway Deploy Log 顯示 `[start] prisma db push...` 且沒有紅色錯誤
- [ ] Deploy Log 顯示 `[start] seeding...` 後出現 `DB 已有 N 筆月份，略過`（**若沒看到這行請立刻停下來檢查**，代表資料庫被判定為空）
- [ ] 健康檢查通過，網站可正常登入

---

## 階段 4 — 部署後立即驗證（不做任何修改，只看）

- [ ] `/dashboard` 正常顯示，數字與階段 2 記錄**完全一致**
- [ ] `/report/2025` 年報數字與階段 2 記錄**完全一致**
- [ ] 隨機打開一個既有月份 `/monthly/[年]/[月]`：
  - [ ] 所有明細列都在，金額正確
  - [ ] 項目名稱下拉顯示為 `原名稱（既有）` 並已選中 ← **這是最關鍵的一項**
  - [ ] 沒有任何列出現「貨主／薪資／上期」標籤（既有資料應全為手動來源）
- [ ] `/suppliers` 與 `/payroll` 既有資料完整

> 若任一項不符，**立即停止，用階段 1.3 回滾**。

---

## 階段 5 — 初始化名稱字典（**必做，否則下拉是空的**）

Railway → service → **Shell** 分頁，執行：

```bash
npm run db:presets
```

- [ ] 輸出顯示三個 scope 的新增筆數（LINE_ITEM / SUPPLIER / COST_CENTER）
- [ ] 再執行一次，三個 scope 的「新增」都是 0（確認冪等）
- [ ] `/settings/presets` 可看到各分類的既有名稱與使用次數

---

## 階段 6 — 在「新月份」試操作（不碰既有資料）

先開一個全新月份當實驗場，例如 2026/09。

- [ ] `/monthly/new` 建立新月份，**勾選**「自動帶入上一期固定支出」
- [ ] 建立後看到提示「已自動帶入 YYYY/MM 的固定支出 N 筆」（FIX-8）
- [ ] 固定支出列已帶入**含金額**，標籤顯示「上期」
- [ ] `/suppliers` 切到該月 → 輸入 2 筆貨主 → 儲存 → 按「匯入至損益表」
- [ ] 回該月損益表：營業成本出現 2 筆、標籤「貨主」，小計正確
- [ ] 回 `/suppliers` 改一筆金額 → 再匯入一次 → 損益表**仍是 2 筆**（非 4 筆），金額已更新
- [ ] `/payroll` 同樣流程試一次，人事薪資標籤「薪資」
- [ ] 確認無誤後，可刪除這個測試月份

---

## 階段 7 — 既有月份第一次使用匯入（**風險最高，逐月做**）

> ⚠️ 匯入**不會**刪除你手動輸入的列。既有月份如果本來就手動打了薪資或貨主明細，
> 匯入後會變成**兩份、金額雙倍**。系統會提示「另有 N 筆手動輸入」，但要你自己清掉。

每個月份請照這個順序：

1. [ ] 先記下該月的「本期淨利」
2. [ ] 執行匯入
3. [ ] 看到「另有 N 筆手動輸入的…請確認是否重複」提示時，**逐筆檢查並刪除重複的手動列**
4. [ ] 儲存後確認「本期淨利」符合預期（若原本就有手動列，數字應在刪除重複列後回到原值）
5. [ ] 確認無誤才處理下一個月份

**建議一次只處理一個月份，確認完再做下一個。**

---

## 階段 8 — 名稱合併（可延後，不急）

`IMPORT_CARRY_SPEC.md` 附錄 A 有建議合併清單（如 `油資` → `車輛_加油費`）。

- [ ] 合併前再做一次階段 1 的備份
- [ ] 在 `/settings/presets` 逐條操作，**勾選「同步更新既有資料」才會改動歷史資料**
- [ ] 每合併一條，就到 `/report/2025` 確認該列真的併成一列、金額為兩者相加
- [ ] **不要**碰附錄 A 標註警告的兩組（富育貸款群、車貸群）—— 那可能是不同筆貸款，需先確認

---

## 出問題時

| 症狀 | 處理 |
|---|---|
| 部署後數字對不上 | 立即回滾（階段 1.3），不要嘗試手動修 |
| 下拉選單是空的 | 沒跑階段 5 的 `npm run db:presets` |
| 匯入後金額變雙倍 | 正常現象，去刪除重複的手動列（階段 7 步驟 3） |
| 舊名稱顯示成「（既有）」 | 正常設計，不是錯誤。要收斂請走階段 8 |
| Deploy Log 沒出現「DB 已有 N 筆月份，略過」 | **立即停止**，資料庫可能被判定為空並寫入種子資料，回滾 |
