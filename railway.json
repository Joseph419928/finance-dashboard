{
  "$schema": "https://railway.app/railway.schema.json",
  "build": { "builder": "DOCKERFILE" },
  "deploy": {
    "startCommand": "node node_modules/prisma/build/index.js migrate deploy && (node prisma/seed-prod.cjs || true) && node server.js",
    "restartPolicyType": "ON_FAILURE"
  }
}
